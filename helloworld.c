#include<stdio.h>

//avant de declarer une fonction il faut determiner son type comme dans une variable , ex pour la fonction main comme dans f(x) qui sera stocke dans y avec pour variable x , f est la fonction//
 
 int main ()
 {
 	printf("Hello world!");
 	printf("\nBonjour le monde\n");
	return (0); 
 }
